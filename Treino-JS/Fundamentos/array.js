const valores = [1, 2, 3, 4, 5];
    
console.log(valores[0]); /*Dentro do console eu coloca a var/const/let
depois vou pegar a posição específica dentro do array (que no caso foi a posição 0 que é = a 1) */
console.log(typeof valores); //Vejo qual o tipo de dado é o array
delete valores[0]; //Deletei a primeira posição do array 
console.log(valores);
